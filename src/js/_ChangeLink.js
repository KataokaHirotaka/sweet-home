export default class ChangeLink {
  constructor() {
    this.breadbrumb = document.getElementById("js-breadbcrumb");
    this.path = location.pathname;
  }

  init() {
    // this.createBreadcrumbHtml();
    // if (this.path)
  }

  createLinkHtml() {}
}
